# Core dependencies
pandas>=1.3.0
numpy>=1.20.0
matplotlib>=3.4.0
ta>=0.7.0
plotly>=5.10.0
mplfinance>=0.12.9b0  # For better candlestick charts
requests>=2.25.1  # For API calls

# Binance integration
python-binance>=1.0.16
websockets>=10.0
pycryptodome>=3.10.0
aiohttp>=3.8.0

# Streamlit UI
streamlit>=1.22.0

# Optional dependencies (Windows only)
# MetaTrader5>=5.0.33 ; sys_platform == 'win32'
# pymt5adapter>=0.4.4 ; sys_platform == 'win32'

# UI dependencies (may require additional setup)
# PySide2>=5.15.0 